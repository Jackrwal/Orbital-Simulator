<Page x:Class="OrbitalSimulator_UI.Views.CanvasPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:OrbitalSimulator_UI.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CanvasPage"
      Background="White">

    <Grid>
        <Label Content="Test123" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black"/>
        <Canvas>
            <Ellipse Width="{Binding Width}" Height="{Binding Height}" Canvas.Left="{Binding Position.X}" Canvas.Top="{Binding Position.Y}" Fill="Red"/>
        </Canvas>
    </Grid>
</Page>
